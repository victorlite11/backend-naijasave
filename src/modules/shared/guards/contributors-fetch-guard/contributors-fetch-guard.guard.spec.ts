import { ContributorsFetchGuard } from './contributors-fetch-guard.guard';

describe('ContributorsFetchGuard', () => {
  it('should be defined', () => {
    expect(new ContributorsFetchGuard()).toBeDefined();
  });
});
